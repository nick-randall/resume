import { FC } from "react";

interface LogoProps {
  
}
 
const Logo: FC<LogoProps> = () => {
  return ( 
    <div style={{fontSize: 30,   color: "#FFF500"
    }}>
      NR
    </div>
   );
}
 
export default Logo;